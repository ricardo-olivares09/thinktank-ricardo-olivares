Claro. Te dejo un **instructivo operativo** para tus colaboradores de investigación y después un **árbol del proyecto** como debería quedar.

---

# Instructivo para colaboradores de investigación del IMEG

## Objetivo

Publicar artículos o preparar datasets **sin tocar el código sensible del sitio**.

## Regla principal

**No editar estas carpetas:**

```bash
app/
lib/
```

Solo trabajarán con:

```bash
content/
public/
```

y con la herramienta interna:

```bash
/editor-interno
```

---

## A) Cómo generar un artículo

### 1. Abrir la herramienta interna

Con el proyecto corriendo localmente, entrar a:

```bash
http://localhost:3000/editor-interno
```

Elegir:

```bash
Generar artículo
```

---

### 2. Llenar los campos

Completar:

* Título
* Resumen
* Autor
* Categoría
* Fecha
* Si será destacada o no

---

### 3. Construir el cuerpo del texto

Usar los botones:

* **Agregar párrafo**
* **Agregar subtítulo**
* **Agregar imagen**

#### Para imágenes

Subir la imagen y escribir el pie de imagen.

No deben escribir rutas manualmente.

---

### 4. Descargar el paquete del artículo

Dar clic en:

```bash
Descargar paquete del artículo
```

Esto descargará un `.zip`.

---

### 5. Descomprimir el zip

Al abrirlo verán una estructura como esta:

```bash
content/publicaciones/slug-del-articulo.md
public/publicaciones/slug-del-articulo/imagen-1.png
public/publicaciones/slug-del-articulo/imagen-2.png
```

---

### 6. Copiar archivos al proyecto

Copiar:

#### El artículo:

```bash
content/publicaciones/slug-del-articulo.md
```

#### Las imágenes:

```bash
public/publicaciones/slug-del-articulo/
```

---

## B) Cómo generar un dataset

### 1. Abrir la herramienta interna

Entrar a:

```bash
http://localhost:3000/editor-interno
```

Elegir:

```bash
Generar dataset
```

---

### 2. Llenar los campos

Completar:

* Título del dataset
* Categoría
* Resumen
* Descripción
* Fuente primaria
* Metodología y transformación IMEG
* Frecuencia
* Cobertura
* Última actualización
* Variables clave
* Visibilidad
* Subir el CSV

---

### 3. Descargar el paquete del dataset

Dar clic en:

```bash
Descargar paquete del dataset
```

Esto descargará un `.zip`.

---

### 4. Descomprimir el zip

Verán algo así:

```bash
content/datasets/slug-del-dataset.json
content/datasets/slug-del-dataset-cita.txt
public/datasets/slug-del-dataset.csv
```

---

### 5. Copiar archivos al proyecto

Copiar:

#### Metadatos:

```bash
content/datasets/slug-del-dataset.json
```

#### Archivo CSV:

```bash
public/datasets/slug-del-dataset.csv
```

#### Cita:

```bash
content/datasets/slug-del-dataset-cita.txt
```

Ese `.txt` es solo de apoyo interno; no rompe nada.

---

## C) Cómo subir cambios al proyecto

Después de copiar los archivos correctos:

### 1. Abrir terminal en la carpeta del proyecto

```bash
cd "C:\Users\oliva\Documents\thinktank"
```

### 2. Revisar cambios

```bash
git status
```

### 3. Agregar cambios

```bash
git add .
```

### 4. Crear commit

```bash
git commit -m "Agrega nuevo artículo o dataset"
```

### 5. Subir a GitHub

```bash
git push origin main
```

Después de eso, **Vercel actualiza el sitio automáticamente**.

---

## D) Qué no deben hacer

No deben:

* editar `layout.tsx`
* editar `page.tsx` de rutas públicas
* cambiar navegación
* cambiar colores
* mover carpetas
* cambiar nombres de `app/` o `lib/`
* subir zips al repo

### Importante

**Al repo no se sube el zip.**
Solo se suben los archivos ya descomprimidos y colocados en su carpeta correcta.

---

## E) Dónde va cada cosa

### Artículos

```bash
content/publicaciones/
```

### Imágenes de artículos

```bash
public/publicaciones/slug-del-articulo/
```

### Datasets metadata

```bash
content/datasets/
```

### CSV de datasets

```bash
public/datasets/
```

### Fotos de colaboradores

```bash
public/colaboradores/
```

---

# Árbol recomendado del proyecto

```bash
thinktank/
├── app/
│   ├── layout.tsx
│   ├── editor-interno/
│   │   └── page.tsx
│   └── (site)/
│       ├── layout.tsx
│       ├── page.tsx
│       ├── instituto/
│       │   └── page.tsx
│       ├── publicaciones/
│       │   ├── page.tsx
│       │   └── [slug]/
│       │       └── page.tsx
│       ├── datos/
│       │   ├── page.tsx
│       │   └── [slug]/
│       │       └── page.tsx
│       ├── mapas/
│       │   └── page.tsx
│       ├── proyectos/
│       │   └── page.tsx
│       ├── colaboradores/
│       │   ├── page.tsx
│       │   └── [slug]/
│       │       └── page.tsx
│       └── panel/
│           └── page.tsx
│
├── content/
│   ├── publicaciones/
│   │   ├── nearshoring-con-evidencia.md
│   │   └── choques-logisticos-en-mexico.md
│   └── datasets/
│       ├── exportaciones-mexico-estados-unidos-por-sector.json
│       ├── exportaciones-mexico-estados-unidos-por-sector-cita.txt
│       ├── eventos-anomalos-y-choques-logisticos.json
│       ├── eventos-anomalos-y-choques-logisticos-cita.txt
│       ├── infraestructura-estrategica-y-nodos-logisticos.json
│       └── infraestructura-estrategica-y-nodos-logisticos-cita.txt
│
├── lib/
│   ├── categorias.ts
│   ├── colaboradores.ts
│   ├── proyectos.ts
│   ├── publicaciones-content.ts
│   └── datasets-content.ts
│
├── public/
│   ├── logo-imeg.png
│   ├── colaboradores/
│   │   ├── david-barkin.jpg
│   │   ├── carlos-silva.jpg
│   │   └── ricardo-olivares.jpg
│   ├── publicaciones/
│   │   ├── nearshoring-con-evidencia/
│   │   │   ├── imagen-1.png
│   │   │   └── imagen-2.png
│   │   └── choques-logisticos-en-mexico/
│   │       └── imagen-1.png
│   └── datasets/
│       ├── exportaciones-mexico-estados-unidos-por-sector.csv
│       ├── eventos-anomalos-y-choques-logisticos.csv
│       └── infraestructura-estrategica-y-nodos-logisticos.csv
│
├── package.json
├── tsconfig.json
├── next.config.ts
└── README.md
```

---

# Flujo resumido para ellos

## Artículo

1. abrir `/editor-interno`
2. generar artículo
3. descargar zip
4. descomprimir
5. copiar `.md` a `content/publicaciones/`
6. copiar imágenes a `public/publicaciones/slug/`
7. `git add .`
8. `git commit`
9. `git push`

## Dataset

1. abrir `/editor-interno`
2. generar dataset
3. descargar zip
4. descomprimir
5. copiar `.json` a `content/datasets/`
6. copiar `.csv` a `public/datasets/`
7. `git add .`
8. `git commit`
9. `git push`

---

# Recomendación final

Conviene que este instructivo también lo guardes dentro del proyecto como archivo, por ejemplo:

```bash
GUIA_COLABORADORES_IMEG.md
```

Así lo tienen siempre a la mano.

Puedo dejarte ese archivo ya redactado, listo para copiar y pegar.
